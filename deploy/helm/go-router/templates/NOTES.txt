1. Wait for the deployment:

   kubectl rollout status deployment/{{ include "go-router.fullname" . }} -n {{ .Release.Namespace }}

2. Open the service:
{{- if eq .Values.service.type "LoadBalancer" }}

   kubectl get service {{ include "go-router.fullname" . }} -n {{ .Release.Namespace }}
{{- else }}

   kubectl port-forward service/{{ include "go-router.fullname" . }} 3000:{{ .Values.service.port }} -n {{ .Release.Namespace }}
   Open http://127.0.0.1:3000/admin
{{- end }}
